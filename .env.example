# Cyna Frontend — variables d'environnement (exemple)
# Copier en .env et adapter. Le front Angular consomme l'API via le proxy /api -> :8080.

API_URL=http://localhost:8080
JWT_SECRET=your-secret-key
STRIPE_PUBLIC_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxx
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
